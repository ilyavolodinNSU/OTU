# Лабораторная работа №10.5
## Построение дискретных аналогов систем управления

---

### Задание:
1. Построить дискретные аналоги систем управления из 10.2
2. Исследовать зависимости точности дискретизации от шага временной сетки, типа регулятора и способа дискретизации

---

## 1. Построение передаточных функций при T=0

### 1.1 Для ПИ-регулятора

Исходные данные:
- $K = 0.46875$
- $T_i = 2.25$
- $n = 3$
- $T_0 = 1.18$

Передаточная функция ПИ-регулятора:

$$W_{ПИ}=K\left(1+\frac{1}{T_{i}s}\right)= 0.46875\left(1+\frac{1}{2.25s}\right)$$

С учетом объекта управления:

$$W_{obj} = \frac{2}{(1 + 1.18s)^3}$$

Замкнутая система:

$$W = \frac{W_{ПИ} \cdot W_{obj}}{1+W_{ПИ} \cdot W_{obj}}$$

**Код для Scilab:**
```scilab
clear;
clc;

s = poly(0, "s");

K_pi = 0.46875;
Ti = 2.25;
T0 = 1.18;
n = 3;

W_obj = 2 / (1 + T0*s)^n;
W_PI_reg = K_pi * (1 + 1/(Ti*s));
W_PI = syslin('c', (W_PI_reg * W_obj) / (1 + W_PI_reg * W_obj));

sys_ss_PI = tf2ss(W_PI);
disp("A = "); disp(sys_ss_PI.A);
disp("B = "); disp(sys_ss_PI.B);
disp("C = "); disp(sys_ss_PI.C);
disp("D = "); disp(sys_ss_PI.D);
```

**Результат — передаточная функция:**

$$W_{ПИ}(s) = \frac{0.5706s + 0.2536}{s^4 + 2.5424s^3 + 2.1546s^2 + 1.1792s + 0.2536}$$

**Описание в нормальной форме первого порядка:**

$$\dot{x} = Ax + Bu, \quad y = Cx + Du$$

$$A = \begin{pmatrix} 0.1068 & 0.5258 & 0 & 0 \\ -0.0217 & 0.2796 & 1.8710 & 0 \\ -0.7243 & -1.1546 & -2.4823 & 0.0264 \\ 0.1144 & -0.6315 & 0.8907 & -0.4466 \end{pmatrix}$$

$$B = \begin{pmatrix} 0 \\ 0 \\ -0.9049 \\ 0.0726 \end{pmatrix}, \quad C = \begin{pmatrix} -0.6410 & 0 & 0 & 0 \end{pmatrix}, \quad D = 0$$

### 1.2 Для ПИД-регулятора

Исходные данные:
- $K = 2.6$
- $T_i = 4$
- $T_d = T_i/4 = 1$
- $T_c = T_d/8 = 0.125$

**Код для Scilab:**
```scilab
K_pid = 2.6;
Ti_pid = 4;
Td_pid = Ti_pid/4;
Tc_pid = Td_pid/8;

W_PID_reg = K_pid * (1 + 1/(Ti_pid*s) + (Td_pid*s)/(1 + Tc_pid*s));
W_PID = syslin('c', (W_PID_reg * W_obj) / (1 + W_PID_reg * W_obj));

sys_ss_PID = tf2ss(W_PID);
disp("A = "); disp(sys_ss_PID.A);
disp("B = "); disp(sys_ss_PID.B);
disp("C = "); disp(sys_ss_PID.C);
disp("D = "); disp(sys_ss_PID.D);
```

**Результат — передаточная функция:**

$$W_{ПИД}(s) = \frac{28.484 s^2 + 26.110 s + 6.330}{s^5 + 10.542 s^4 + 22.494 s^3 + 46.329 s^2 + 30.979 s + 6.330}$$

**Описание в нормальной форме первого порядка:**

$$A = \begin{pmatrix} 0.5498 & -2.8259 & 0 & 0 & 0 \\ 0.0684 & 0.3565 & 3.9670 & 0 & 0 \\ 3.5071 & -5.7088 & -10.5302 & -0.0915 & 0 \\ 0.5014 & 2.2182 & -0.6719 & -0.3480 & -0.0358 \\ -0.3356 & -1.4520 & 0.2542 & 0.6902 & -0.5704 \end{pmatrix}$$

$$B = \begin{pmatrix} 0 \\ 0 \\ 7.8038 \\ 0.1495 \\ -0.0487 \end{pmatrix}, \quad C = \begin{pmatrix} -0.3256 & 0 & 0 & 0 & 0 \end{pmatrix}, \quad D = 0$$

---

## 2. Дискретизация систем

Дискретизация выполнена методом удержания нулевого порядка (ZOH) с шагом $\tau = 0.1$ с. Дискретный аналог описывается системой разностных уравнений в нормальной форме первого порядка:

$$x_{k+1} = A_d x_k + B_d u_k$$
$$y_k = C_d x_k + D_d u_k$$

### 2.1 Дискретный ПИ-регулятор

```scilab
h = 0.1;
Sys_PI_disc = dscr(sys_ss_PI, h);
disp("A_d = "); disp(Sys_PI_disc.A);
disp("B_d = "); disp(Sys_PI_disc.B);
disp("C_d = "); disp(Sys_PI_disc.C);
disp("D_d = "); disp(Sys_PI_disc.D);
```

**Матрицы дискретной системы:**

$$A_d = \begin{pmatrix} 1.0106 & 0.0534 & 0.0046 & 0.0000 \\ -0.0085 & 1.0180 & 0.1675 & 0.0002 \\ -0.0641 & -0.1052 & 0.7709 & 0.0023 \\ 0.0085 & -0.0669 & 0.0714 & 0.9564 \end{pmatrix}$$

$$B_d = \begin{pmatrix} -0.0001 \\ -0.0079 \\ -0.0798 \\ 0.0036 \end{pmatrix}, \quad C_d = \begin{pmatrix} -0.6410 & 0 & 0 & 0 \end{pmatrix}, \quad D_d = 0$$

**Первые 10 точек переходной характеристики:**

| $t$, с | $y_k$ |
|--------|-------|
| 0.0    | 0.0000 |
| 0.1    | 0.0001 |
| 0.2    | 0.0007 |
| 0.3    | 0.0022 |
| 0.4    | 0.0050 |
| 0.5    | 0.0092 |
| 0.6    | 0.0151 |
| 0.7    | 0.0228 |
| 0.8    | 0.0324 |
| 0.9    | 0.0439 |

![](10.5.1.png)

### 2.2 Дискретный ПИД-регулятор

```scilab
Sys_PID_disc = dscr(sys_ss_PID, h);
disp("A_d = "); disp(Sys_PID_disc.A);
disp("B_d = "); disp(Sys_PID_disc.B);
disp("C_d = "); disp(Sys_PID_disc.C);
disp("D_d = "); disp(Sys_PID_disc.D);
```

**Матрицы дискретной системы:**

$$A_d = \begin{pmatrix} 1.0502 & -0.2867 & -0.0412 & 0.0001 & 0 \\ 0.0581 & 0.9472 & 0.2408 & -0.0013 & 0 \\ 0.2138 & -0.3837 & 0.2874 & -0.0053 & 0.0000 \\ 0.0470 & 0.2227 & -0.0085 & 0.9658 & -0.0034 \\ -0.0319 & -0.1323 & -0.0061 & 0.0659 & 0.9444 \end{pmatrix}$$

$$B_d = \begin{pmatrix} -0.0116 \\ 0.1116 \\ 0.4638 \\ 0.0050 \\ -0.0033 \end{pmatrix}, \quad C_d = \begin{pmatrix} -0.3256 & 0 & 0 & 0 & 0 \end{pmatrix}, \quad D_d = 0$$

**Первые 10 точек переходной характеристики:**

| $t$, с | $y_k$ |
|--------|-------|
| 0.0    | 0.0000 |
| 0.1    | 0.0038 |
| 0.2    | 0.0244 |
| 0.3    | 0.0674 |
| 0.4    | 0.1324 |
| 0.5    | 0.2163 |
| 0.6    | 0.3146 |
| 0.7    | 0.4226 |
| 0.8    | 0.5357 |
| 0.9    | 0.6493 |

![](10.5.2.png)

---

## 3. Переходные характеристики в Micro-Cap

Для получения эталонных характеристик непрерывных систем использовалась программа Micro-Cap Demo. Числовой вывод данных включён в свойствах графика. Результаты экспортированы в CSV-файлы для дальнейшего сравнения.

### 3.1 ПИ-регулятор в Micro-Cap

![](10.5.3.png)

### 3.2 ПИД-регулятор в Micro-Cap

![](10.5.4.png)

---

## 4. Расчёт переходных характеристик дискретных аналогов в Scilab

```scilab
function compare_models(reg_type, h_step)
    
    T0 = 1.18;
    n = 3;
    
    if reg_type == "PI" then
        K = 0.46875;
        Ti = 2.25;
        csv_file = "C:\Users\6ic47\OneDrive\Рабочий стол\10.2\circuit2_PI.CSV";
        title_str = "ПИ-регулятор";
    else
        K = 2.6;
        Ti = 4;
        Td = Ti/4;
        Tc = Td/8;
        csv_file = "C:\Users\6ic47\OneDrive\Рабочий стол\10.2\circuit2_PID.CSV";
        title_str = "ПИД-регулятор";
    end
    
    s = poly(0, 's');
    W_obj = 2 / (1 + T0*s)^n;
    
    if reg_type == "PI" then
        W_reg = K * (1 + 1/(Ti*s));
    else
        W_reg = K * (1 + 1/(Ti*s) + (Td*s)/(1+Tc*s));
    end
    
    W_cl = (W_reg * W_obj) / (1 + W_reg * W_obj);
    sys_cont = syslin('c', W_cl);
    sys_disc = dscr(sys_cont, h_step);
    
    t_max = 100;
    t_disc = 0:h_step:t_max;
    N = length(t_disc);
    
    nx = size(sys_disc.A, 1);
    x = zeros(nx, 1);
    u = ones(1, N);
    h_disc = zeros(1, N);
    
    for i = 1:N
        x = sys_disc.A * x + sys_disc.B * u(i);
        h_disc(i) = sys_disc.C * x;
    end
    
    data = csvRead(csv_file);
    t_mc = data(:, 1);
    y_mc = data(:, 2);
    dt_mc = t_mc(2) - t_mc(1);
    
    err_sum = 0;
    for k = 1:N
        time_val = (k-1) * h_step;
        row_idx = round(time_val / dt_mc) + 1;
        if row_idx > length(y_mc) then
            row_idx = length(y_mc);
        end
        err_sum = err_sum + (y_mc(row_idx) - h_disc(k))^2;
    end
    
    error_rms = sqrt(err_sum / N);
    
    scf();
    plot(t_disc, h_disc, 'r-', 'LineWidth', 2);
    plot(t_mc, y_mc, 'b-');
    xlabel('t, c');
    ylabel('x(t)');
    title(title_str + " (h = " + string(h_step) + " c)");
    legend(['Scilab (дискретная)'; 'Micro-Cap (непрерывная)'], "lowerright");
    xgrid();
    
    mprintf("\n=== " + title_str + " ===\n");
    mprintf("Шаг дискретизации tau = %.1f c\n", h_step);
    mprintf("Среднеквадратическая ошибка e = %.5f\n\n", error_rms);
    
endfunction

compare_models("PI", 0.1);
compare_models("PI", 0.5);
compare_models("PI", 1.0);
compare_models("PID", 0.1);
compare_models("PID", 0.5);
compare_models("PID", 1.0);
```

---

## 5. Сравнение переходных характеристик по норме ошибки

Для оценки точности дискретизации использовалась среднеквадратическая ошибка:

$$e = \sqrt{\frac{1}{N}\sum_{k=1}^{N}[h((k-1)\tau) - h_{d,k}]^{2}}$$

где:
- $h(t)$ — переходная характеристика непрерывного регулятора (Micro-Cap)
- $h_{d,k}$ — переходная характеристика дискретного аналога (Scilab)
- $\tau$ — шаг временной сетки

### Результаты для ПИ-регулятора:

| Шаг $\tau$, с | Ошибка $e$ |
|---------------|------------|
| 0.1           | 0.05763    |
| 0.5           | 0.03797    |
| 1.0           | 0.02480    |

![](10.5.5.png)
*Рис. 5.1 — Сравнение для ПИ при h = 0.1*

![](10.5.6.png)
*Рис. 5.2 — Сравнение для ПИ при h = 0.5*

![](10.5.7.png)
*Рис. 5.3 — Сравнение для ПИ при h = 1.0*

### Результаты для ПИД-регулятора:

| Шаг $\tau$, с | Ошибка $e$ |
|---------------|------------|
| 0.1           | 0.04088    |
| 0.5           | 0.02382    |
| 1.0           | 0.01960    |

![](10.5.8.png)
*Рис. 5.4 — Сравнение для ПИД при h = 0.1*

![](10.5.9.png)
*Рис. 5.5 — Сравнение для ПИД при h = 0.5*

![](10.5.10.png)
*Рис. 5.6 — Сравнение для ПИД при h = 1.0*

---

## 6. Исследование зависимости ошибки дискретизации от шага $\tau$

### 6.1 Код для исследования зависимости

```scilab
clear;
clc;

h_values = [0.01, 0.05, 0.1, 0.2, 0.5, 0.75, 1.0, 1.5, 2.0];
error_PI = zeros(h_values);
error_PID = zeros(h_values);

T0 = 1.18;
n = 3;
s = poly(0, 's');
W_obj = 2 / (1 + T0*s)^n;

K_pi = 0.46875;
Ti_pi = 2.25;

K_pid = 2.6;
Ti_pid = 4;
Td_pid = Ti_pid/4;
Tc_pid = Td_pid/8;

data_PI = csvRead("C:\Users\6ic47\OneDrive\Рабочий стол\10.2\circuit2_PI.CSV");
t_mc_PI = data_PI(:, 1);
y_mc_PI = data_PI(:, 2);
dt_mc_PI = t_mc_PI(2) - t_mc_PI(1);

data_PID = csvRead("C:\Users\6ic47\OneDrive\Рабочий стол\10.2\circuit2_PID.CSV");
t_mc_PID = data_PID(:, 1);
y_mc_PID = data_PID(:, 2);
dt_mc_PID = t_mc_PID(2) - t_mc_PID(1);

W_PI_reg = K_pi * (1 + 1/(Ti_pi*s));
W_PI_cl = (W_PI_reg * W_obj) / (1 + W_PI_reg * W_obj);

W_PID_reg = K_pid * (1 + 1/(Ti_pid*s) + (Td_pid*s)/(1+Tc_pid*s));
W_PID_cl = (W_PID_reg * W_obj) / (1 + W_PID_reg * W_obj);

for i = 1:length(h_values)
    h = h_values(i);
    
    sys_PI_disc = dscr(W_PI_cl, h);
    t_max = 100;
    t_disc = 0:h:t_max;
    N = length(t_disc);
    nx = size(sys_PI_disc.A, 1);
    x = zeros(nx, 1);
    u = ones(1, N);
    h_disc = zeros(1, N);
    for k = 1:N
        x = sys_PI_disc.A * x + sys_PI_disc.B * u(k);
        h_disc(k) = sys_PI_disc.C * x;
    end
    err_sum = 0;
    for k = 1:N
        time_val = (k-1) * h;
        row_idx = round(time_val / dt_mc_PI) + 1;
        if row_idx > length(y_mc_PI) then
            row_idx = length(y_mc_PI);
        end
        err_sum = err_sum + (y_mc_PI(row_idx) - h_disc(k))^2;
    end
    error_PI(i) = sqrt(err_sum / N);
    
    sys_PID_disc = dscr(W_PID_cl, h);
    nx = size(sys_PID_disc.A, 1);
    x = zeros(nx, 1);
    h_disc = zeros(1, N);
    for k = 1:N
        x = sys_PID_disc.A * x + sys_PID_disc.B * u(k);
        h_disc(k) = sys_PID_disc.C * x;
    end
    err_sum = 0;
    for k = 1:N
        time_val = (k-1) * h;
        row_idx = round(time_val / dt_mc_PID) + 1;
        if row_idx > length(y_mc_PID) then
            row_idx = length(y_mc_PID);
        end
        err_sum = err_sum + (y_mc_PID(row_idx) - h_disc(k))^2;
    end
    error_PID(i) = sqrt(err_sum / N);
    
    mprintf("h = %.2f c: e_PI = %.5f, e_PID = %.5f\n", h, error_PI(i), error_PID(i));
end

scf();
plot(h_values, error_PI, 'ro-', 'LineWidth', 2, 'MarkerSize', 8);
plot(h_values, error_PID, 'bs-', 'LineWidth', 2, 'MarkerSize', 8);
xlabel('Шаг дискретизации \tau, c', 'FontSize', 12);
ylabel('Среднеквадратическая ошибка e', 'FontSize', 12);
title('Зависимость ошибки дискретизации от шага \tau', 'FontSize', 14);
legend(['ПИ-регулятор'; 'ПИД-регулятор'], "upperright");
xgrid();
```

### 6.2 Результаты исследования

![](10.5.11.png)
*Рис. 6.1 — Зависимость ошибки дискретизации от шага $\tau$*

### 6.3 Анализ полученных зависимостей

#### 1. Поведение ПИД-регулятора

Для ПИД-регулятора наблюдается **монотонный рост ошибки** с увеличением шага дискретизации:
- При $\tau = 0.01$ с: $e \approx 0.073$
- При $\tau = 1.0$ с: $e \approx 0.123$
- При $\tau = 2.0$ с: $e \approx 0.174$

ПИД-регулятор содержит дифференцирующую составляющую, которая особенно чувствительна к шагу дискретизации. При больших $\tau$ теряется информация о скорости изменения сигнала, что приводит к возрастанию ошибки аппроксимации.

#### 2. Поведение ПИ-регулятора

Для ПИ-регулятора наблюдается **немонотонная зависимость с выраженным минимумом**:
- При $\tau = 0.01$ с: $e \approx 0.048$
- При $\tau = 0.5$ с: $e \approx 0.025$
- При $\tau = 1.0$ с: $e \approx 0.018$ (минимум)
- При $\tau = 2.0$ с: $e \approx 0.062$

Существование оптимального шага $\tau_{opt} \approx 1.0$ с объясняется действием двух противофазных факторов:

**а) При очень малых шагах ($\tau < 0.5$ с):**
- Происходит накопление вычислительных погрешностей округления
- Интегральная составляющая суммирует большое количество малых значений, что увеличивает ошибку квантования

**б) При больших шагах ($\tau > 1.0$ с):**
- Проявляется эффект наложения спектров (алиасинг)
- Теряется информация о динамике переходного процесса
- Увеличивается ошибка аппроксимации непрерывного интеграла дискретной суммой

**в) Оптимальная область ($\tau \approx 0.75...1.25$ с):**
- Достигается баланс между точностью дискретизации и вычислительной погрешностью
- Шаг согласуется с постоянной времени системы $T_i = 2.25$ с ($\tau_{opt} \approx T_i/2$)

#### 3. Сравнительный анализ регуляторов

**В области малых шагов ($\tau < 0.5$ с):**
- ПИ-регулятор точнее (ошибка в 1.5–2 раза меньше)
- Объясняется отсутствием чувствительной дифференциальной составляющей

**В области больших шагов ($\tau > 1.5$ с):**
- ПИ-регулятор также показывает лучшую точность
- ПИД-регулятор быстро деградирует из-за потери информации о производной

**Выводы:**

Для ПИ-регулятора:
- Оптимальный шаг: $\tau = 0.75...1.25$ с
- Минимальная достижимая ошибка: $e_{min} \approx 0.018$

Для ПИД-регулятора:
- Рекомендуемый шаг: $\tau \leq 0.5$ с
- При $\tau > 1.0$ с ошибка превышает допустимые значения
- Требуется более частая дискретизация из-за дифференциальной составляющей
